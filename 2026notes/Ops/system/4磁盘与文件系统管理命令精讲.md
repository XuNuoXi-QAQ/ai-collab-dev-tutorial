---
title: "4磁盘与文件系统管理命令精讲"
tags:
  - Linux
  - 磁盘
  - 文件系统
  - 运维
  - 命令
created: 2026-06-23
---
# 磁盘与文件系统管理命令精讲

磁盘管理是 Linux 系统运维中最常用的操作之一。这组命令帮助你查看磁盘使用情况、管理分区、处理交换空间，以及诊断磁盘问题。

---

## 一、核心概念

### 磁盘 vs 分区 vs 文件系统
| 概念 | 说明 | 示例 |
|------|------|------|
| **物理磁盘** | 硬盘硬件 | `/dev/sda`、`/dev/nvme0n1` |
| **分区** | 磁盘上的逻辑划分 | `/dev/sda1`、`/dev/sda2` |
| **文件系统** | 分区上的数据组织方式 | ext4、NTFS、FAT32 |
| **挂载点** | 文件系统在系统中的访问位置 | `/`、`/home`、`/boot` |

### 查看你的磁盘
```bash
lsblk
```
输出示例：
```
NAME        MAJ:MIN RM   SIZE RO TYPE MOUNTPOINT
sda           8:0    0   60G  0 disk
├─sda1        8:1    0  512M  0 part /boot/efi
├─sda2        8:2    0 59.5G  0 part /
sr0          11:0    1  1024M  0 rom
```

---

## 2. `df -h` — 查看磁盘分区使用情况（最常用）

### 命令格式
```bash
df -h              # 人类可读格式（GB/MB）
df -hT             # 显示文件系统类型
df -h /home        # 查看特定目录所在分区
```

### 输出示例（你经常用）
```bash
df -h
```
```
文件系统        容量  已用  可用 已用% 挂载点
/dev/sda2        60G   45G   13G   78% /
/dev/sda1       512M   12M  500M    3% /boot/efi
```

### 各部分含义
| 字段 | 含义 | 说明 |
|------|------|------|
| `文件系统` | 分区设备名 | `/dev/sda2` 等 |
| `容量` | 总大小 | 分区的总容量 |
| `已用` | 已使用空间 | 已占用的空间 |
| `可用` | 剩余空间 | 还可以用的空间 |
| `已用%` | **使用率** | 重要指标，>90% 需要清理 |
| `挂载点` | 挂载位置 | 分区在系统中的位置 |

### 常用参数
| 参数 | 用途 | 示例 |
|------|------|------|
| `-h` | 人类可读格式（GB/MB） | `df -h` |
| `-T` | 显示文件系统类型 | `df -hT` |
| `-i` | 显示 inode 使用情况 | `df -i` |
| `-x tmpfs` | 排除 tmpfs 类型 | `df -h -x tmpfs` |

### 实际应用
```bash
# 查看所有分区的使用情况（你最常用的命令之一）
df -h

# 查看 /home 目录所在分区
df -h /home

# 查看 inode 使用情况（文件数量是否用尽）
df -i

# 只查看 ext4 分区
df -h -t ext4
```

---

## 3. `du -sh 目录` — 查看目录总大小

### 命令格式
```bash
du -sh 目录          # 查看目录总大小
du -sh *             # 查看当前目录下所有项目的大小
du -sh ~/文档/diary  # 查看 Obsidian 仓库大小
```

### 参数说明
| 参数 | 含义 |
|------|------|
| `-s` | 只显示总计（不显示子目录） |
| `-h` | 人类可读格式（GB/MB/KB） |
| `--max-depth=N` | 显示 N 层子目录（见下一条） |

### 实际应用
```bash
# 查看你的主目录大小
du -sh /home/nuoxi

# 查看当前目录所有一级子目录大小
du -sh *

# 查看 Obsidian 笔记库大小
du -sh ~/文档/diary
```

---

## 4. `du -h --max-depth=1 目录` — 查看一级子目录大小

### 命令格式
```bash
du -h --max-depth=1 目录    # 显示一级子目录大小
du -h --max-depth=1 | sort -rh   # 按大小排序（最有用）
```

### 实际应用
```bash
# 查看哪个目录占用空间最大（最重要的排查命令）
du -h --max-depth=1 /home/nuoxi | sort -rh

# 查看根目录下哪些文件夹占用空间大
sudo du -h --max-depth=1 / | sort -rh | head -10

# 查看你的项目中哪些目录最大
du -h --max-depth=1 ~/Project | sort -rh
```

### `du` vs `df` 区别
| 命令 | 作用 | 适用场景 |
|------|------|----------|
| `df` | 查看**分区**的整体空间 | 检查分区是否满了 |
| `du` | 查看**目录**的实际大小 | 找出哪个目录占用了空间 |

---

## 5. `lsblk` — 查看块设备信息

### 命令格式
```bash
lsblk           # 树形显示所有块设备
lsblk -f        # 显示文件系统信息
lsblk -m        # 显示权限信息
```

### 输出示例
```
NAME        MAJ:MIN RM   SIZE RO TYPE MOUNTPOINT
sda           8:0    0   60G  0 disk
├─sda1        8:1    0  512M  0 part /boot/efi
├─sda2        8:2    0 59.5G  0 part /
sr0          11:0    1  1024M  0 rom
```

### 各部分含义
| 字段 | 含义 |
|------|------|
| `NAME` | 设备名 |
| `MAJ:MIN` | 设备号（主:次） |
| `RM` | 是否可移除（1=是） |
| `SIZE` | 设备大小 |
| `RO` | 是否只读（1=是） |
| `TYPE` | 类型（disk/part/rom） |
| `MOUNTPOINT` | 挂载点 |

### 实际应用
```bash
# 查看所有磁盘和分区
lsblk

# 查看文件系统类型
lsblk -f

# 查看设备信息（比 fdisk 更直观）
lsblk -o NAME,SIZE,TYPE,MOUNTPOINT
```

---

## 6. `fdisk -l` — 查看磁盘分区表

### 命令格式
```bash
sudo fdisk -l        # 查看所有磁盘分区
sudo fdisk -l /dev/sda  # 查看特定磁盘
```

### 输出示例
```
Disk /dev/sda: 60 GiB, 64424509440 bytes, 125829120 sectors
Disk model: VMware Virtual S
Units: sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 512 bytes
I/O size (minimum/optimal): 512 bytes / 512 bytes
Disklabel type: gpt
Disk identifier: 12345678-1234-1234-1234-123456789abc

Device       Start       End   Sectors  Size Type
/dev/sda1     2048   1050623   1048576  512M EFI System
/dev/sda2  1050624 125829086 124778463 59.5G Linux filesystem
```

### `fdisk` 交互模式（高级）
```bash
sudo fdisk /dev/sda
```
常用交互命令：
| 按键 | 功能 |
|------|------|
| `p` | 显示分区表 |
| `n` | 新建分区 |
| `d` | 删除分区 |
| `w` | 写入并退出 |
| `q` | 退出不保存 |

> ⚠️ **警告**：`fdisk` 操作分区有风险，误操作可能导致数据丢失！

---

## 7. `mount` — 查看已挂载的文件系统

### 命令格式
```bash
mount              # 查看所有挂载
mount | grep sda   # 查看特定设备
mount -t ext4      # 查看特定类型
```

### 输出示例
```
/dev/sda2 on / type ext4 (rw,relatime)
/dev/sda1 on /boot/efi type vfat (rw,relatime,fmask=0022,dmask=0022,codepage=437,iocharset=iso8859-1,shortname=mixed,errors=remount-ro)
```

### 手动挂载/卸载
```bash
# 挂载 U 盘
sudo mount /dev/sdb1 /mnt/usb

# 卸载
sudo umount /mnt/usb
# 或
sudo umount /dev/sdb1
```

---

## 8. 交换空间管理命令

### `swapon -s` — 查看交换空间状态
```bash
swapon -s
```
输出示例：
```
文件名            类型        大小        已用      优先级
/swapfile        file        25165820    0        -2
```

### `free -h` — 查看内存和交换使用（你常用的命令）
```bash
free -h
```
输出示例：
```
             总计        已用        空闲        共享   缓冲/缓存        可用
内存：       12Gi       6.1Gi       4.6Gi       141Mi       2.5Gi       6.7Gi
交换：       23Gi          0B        23Gi
```

### `dd` — 创建大文件（用于交换文件）
```bash
# 创建 4GB 交换文件
sudo dd if=/dev/zero of=/swapfile bs=1M count=4096
```
参数说明：
| 参数 | 含义 |
|------|------|
| `if=/dev/zero` | 输入文件（全零） |
| `of=/swapfile` | 输出文件 |
| `bs=1M` | 块大小 1MB |
| `count=4096` | 块数（1M × 4096 = 4GB） |

### `fallocate` — 快速创建文件（比 dd 快）
```bash
# 创建 4GB 交换文件（推荐）
sudo fallocate -l 4G /swapfile
```

### `mkswap` / `swapon` / `swapoff` — 交换文件操作
```bash
# 1. 创建交换文件
sudo fallocate -l 24G /swapfile
sudo chmod 600 /swapfile

# 2. 格式化为交换空间
sudo mkswap /swapfile

# 3. 启用交换
sudo swapon /swapfile

# 4. 查看状态
swapon -s

# 5. 关闭所有交换（扩容前需要）
sudo swapoff -a
```

### 自动挂载交换文件（/etc/fstab）
```bash
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

---

## 9. `mount -o remount,rw /` — 重新挂载根分区为读写

### 使用场景
系统启动进入救援模式、分区被挂载为只读时需要重新挂载为读写。

### 命令
```bash
sudo mount -o remount,rw /
```

### 常见挂载选项
| 选项 | 含义 |
|------|------|
| `rw` | 读写 |
| `ro` | 只读 |
| `remount` | 重新挂载（不卸载） |
| `noatime` | 不更新访问时间 |
| `nodiratime` | 不更新目录访问时间 |

---

## 📊 磁盘管理命令快速参考卡

| 场景 | 推荐命令 |
|------|----------|
| 查看磁盘使用情况 | `df -h` |
| 查找大目录 | `du -h --max-depth=1 / \| sort -rh \| head -10` |
| 查看磁盘分区 | `lsblk` |
| 查看交换空间 | `swapon -s` |
| 查看内存使用 | `free -h` |
| 创建交换文件 | `sudo fallocate -l 4G /swapfile` |
| 启用交换文件 | `sudo mkswap /swapfile && sudo swapon /swapfile` |

---

## 💡 你的实战场景

### 场景1：磁盘空间不足（你之前遇到的情况）
```bash
# 1. 查看哪个分区满了
df -h
# 发现 / 使用率 93%

# 2. 找出大目录
sudo du -h --max-depth=1 / | sort -rh | head -10

# 3. 清理 Pacman 缓存
sudo paccache -r -k 3

# 4. 清理日志
sudo journalctl --vacuum-size=500M

# 5. 再次确认
df -h
```

### 场景2：扩容交换空间（你之前成功执行过）
```bash
# 1. 关闭旧交换
sudo swapoff -a

# 2. 创建新交换文件（24GB）
sudo fallocate -l 24G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile

# 3. 启用新交换
sudo swapon /swapfile

# 4. 验证
free -h
swapon -s
```